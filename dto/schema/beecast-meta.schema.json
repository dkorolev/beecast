{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Cast metadata sidecar",
  "description": "Sidecar metadata for one `.cast` recording, conventionally stored next to it as\n`<name>.meta.json`. Every field is optional: a bare recording plays fine without any.\nTwo invariants live beyond JSON Schema, in the beecast/seecast validators: chapters\nare STRICTLY ascending by `t`, and a non-empty chapter list MUST start at `t` = 0.",
  "type": "object",
  "properties": {
    "chapters": {
      "description": "Chapter markers, strictly ascending by `t`. When non-empty, the first chapter MUST\nstart at `t: 0` (YouTube-style: the opening segment always has a marker).",
      "type": "array",
      "items": {
        "description": "One chapter marker.",
        "type": "object",
        "properties": {
          "t": {
            "description": "The timekey: seconds into the recording. Fractional values are allowed (`12.5`);\nthe first chapter's `t` must be exactly `0`.",
            "type": "number",
            "format": "double",
            "minimum": 0.0
          },
          "title": {
            "description": "Short chapter title; 3–6 words works best.",
            "type": "string",
            "minLength": 1
          }
        },
        "additionalProperties": false,
        "required": [
          "t",
          "title"
        ]
      }
    },
    "summary": {
      "description": "One- or two-sentence description of what the recording shows, rendered under the title.",
      "type": [
        "string",
        "null"
      ],
      "minLength": 1
    },
    "title": {
      "description": "Short human title for the recording; becomes the page's `<title>` and header.\nAbsent → the page falls back to the recording's filename.",
      "type": [
        "string",
        "null"
      ],
      "minLength": 1
    }
  },
  "additionalProperties": false
}
